# 学習テーマ
作業日時: 2025-11-06


## 目的・背景 
文字列を並び替えても同じものかどうかを判定する。
```bash
FRIED = FIRED
LISTEN = SILENT
```

#### SKILL
```java
char[] c = s.toCharArrary(); // charをarrayに変換する
string c = new String(c);　//char arrayをstring arrayに変換する
```
ー Hash
```java
Map<Integer, Integer> map = new HashMap<>();
map.containKey(value)
map.put(key, vakue)
map.value(key)

// loop throuth hashmap
for(Map.Entry entry: map.entrySet()){
    (int) entry.getKey()
    (int) entry.getValue()
}
```
#### Big-O Runtime and Space Complexity Analysis
runtime: O(3n) -> O(n)
space complexity: O(n)


## 実装内容・学んだ技術  




## 課題・問題点  




## 気づき・改善案  




